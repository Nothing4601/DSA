#include<iostream>
#include<vector>
using namespace std;

void sorting(){

}

int main(){
    int t;
    cin>>t;
    while ((t--))
    {
        int products;
        int vouchers;
        cin>>products;
        cin>>vouchers;
        vector<int>prices(products);
        vector<int>discount(vouchers);

        for (int i = 0; i < products; i++)
           cin>>products[i];
        
        for (int i = 0; i < vouchers; i++)
           cin>>vouchers[i];
        

        

    }   
}